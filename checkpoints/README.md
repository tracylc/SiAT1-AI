# Checkpoint download

The five fold-specific fine-tuned weights (`fold1.pt` to `fold5.pt`) are
available from Zenodo: DOI `10.5281/zenodo.21962712`.

Download the five `.pt` files and place them in this directory before running
`scripts/predict_ensemble.py`.
